# BCSD configuration: CESM2-WACCM / SSP245 / temperature extremes (production)
#
# tasmax/tasmin (and derived dtr) use the corrected historical run and only exist
# for SSP245 members 006-010; members 001-005 are standard-vars-only due to the
# CMIP6 tasmax/tasmin bug. Standard variables live in cesm2-waccm-ssp245-std.yaml.
# Members 007-010's data ends 2069-12-31, so predict_period_end is clamped to 2069 to avoid
# downscaling past their valid extent. Member 006 runs in
# cesm2-waccm-ssp245-member006-2068.yaml instead — see there for why.
#
# Equivalent CLI command:
#   uv run bcsd run --config-path configs/production/

# === BCSDConfig: Run Identity ===
gcm: "CESM2-WACCM"
variables: ["tasmax", "tasmin", "dtr"]
ensemble_members: ["007", "008", "009", "010"]
scenarios: ["SSP245"]

# === Downscaling Methods ===
# Required: there is no default, so every config states which method produced its output.
# Production publishes BCSD only for now; QDMSD is exercised in the qa configs, which list
# both. Adding it here doubles every wave, so it is a deliberate separate decision.
downscaling_methods: ["BCSD", "QDMSD"]

train_period_start: 1978
train_period_end: 2014
predict_period_start: 2015
predict_period_end: 2069

# === PipelineOptions: Operational Settings ===
scratch_dir: "s3://carbonplan-srm/scratch/cache"
output_dir: "s3://us-west-2.opendata.source.coop/carbonplan/srm-downscaling/output/"

environment: "production"
# version intentionally unset — derived from the installed package version at runtime
verbose: true
save_intermediate: true
