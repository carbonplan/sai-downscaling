# BCSD configuration: CESM2-WACCM6 / G6-1.5K / all variables / global (production)
#
# Equivalent CLI command:
#   uv run bcsd run --config-path configs/production/cesm2-waccm6-g6.yaml

# Member 002 is published alongside 003 because it is the realization the termination
# run continues (see cesm2-waccm6-g6-end.yaml). Without it, the only way to compare SAI
# continuing against SAI stopping would cross realizations, which is the ensemble-level
# version of the per-variable mismatch issue #448 hit.

# === DownscalingConfig: Run Identity ===
gcm: "CESM2-WACCM6"
variables: ["tas", "pr", "rsds", "tasmax", "tasmin", "dtr"]
ensemble_members: ["002", "003"]
scenarios: ["G6-1.5K"]

# === Downscaling Methods ===
# Required: there is no default, so every config states which method produced its output.
# Production publishes BCSD only for now; QDMSD is exercised in the qa configs, which list
# both. Adding it here doubles every wave, so it is a deliberate separate decision.
downscaling_methods: ["BCSD", "QDMSD"]

train_period_start: 1978
train_period_end: 2014
predict_period_start: 2035
predict_period_end: 2084

# === PipelineOptions: Operational Settings ===
scratch_dir: "s3://carbonplan-srm/scratch/cache"
output_dir: "s3://us-west-2.opendata.source.coop/carbonplan/srm-downscaling/output/"

environment: "production"
# version intentionally unset — derived from the installed package version at runtime
verbose: true
save_intermediate: true
