# BCSD configuration: CESM2-WACCM / all variables / G6-1.5K / all members / South Africa (QA)

# === Model and Scenario ===
gcm: "CESM2-WACCM"
variables: ["tas", "pr", "rsds", "hurs", "tasmax", "tasmin", "dtr"]
ensemble_members: ["001", "002", "003"]
scenarios: ["G6-1.5K"]

# === Time Periods ===
train_period_start: 1978
train_period_end: 2014
predict_period_start: 2035
predict_period_end: 2084

# === Spatial Subsetting ===
subset_bounds: [-31, -26, 23, 30]

# === Storage ===
scratch_dir: "s3://carbonplan-scratch/srm/bcsd-cache"
output_dir: "s3://carbonplan-scratch/srm/output"
environment: "qa"

# === Additional Options ===
verbose: true
save_intermediate: true
